GITHUB_TOKEN=ghp_your_personal_access_token_here
GITHUB_USERNAME=seonggwon21-tech

# Optional — only needed for the write-path (CRUD) tests (`pytest -m write`).
# The token above must have the `repo` scope, and the fixture will auto-create
# this sandbox repo if it doesn't exist. Read-only tests don't need either.
GITHUB_WRITE_REPO=qa-sandbox
