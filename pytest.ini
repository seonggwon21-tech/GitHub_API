[pytest]
addopts =
    --alluredir=allure-results
    -v

markers =
    user: Tests for User API endpoints
    repos: Tests for Repository API endpoints
    issues: Tests for Issues API endpoints
    write: Write-path CRUD tests — create/update/delete real data (needs a PAT with repo scope; auto-skips without it)
    smoke: Quick smoke tests
